{% extends 'base.html.twig' %}

{% block title %}New Trail Running Event{% endblock %}

{% block body %}
    <div class="form-container">
        <h1>Create new Trail Running Event</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}

        <a href="{{ path('app_trail_running_index_s') }}">Volver</a>
    </div>
{% endblock %}
