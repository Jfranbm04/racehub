{% extends 'base.html.twig' %}

{% block title %}New Trail Running Participant{% endblock %}

{% block body %}
    <div style="margin-bottom: 20px;">
        <a href="{{ path('app_trail_running_participant_index_s') }}" class="btn-primary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>

    <div class="admin-container">
        <header>
            <h1 class="admin-title"><i class="fas fa-running"></i> New Trail Running Participant</h1>
        </header>

        <main>
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn-primary">
                    <i class="fas fa-save"></i> Save
                </button>
            {{ form_end(form) }}
        </main>
    </div>
{% endblock %}
